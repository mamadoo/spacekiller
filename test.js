const spacekiller = require('./'); // require the `index.js` file from the same directory.

const stringWithoutSpaces = spacekiller('So much space!');

const stringWithoutSpaces = spacekiller(255);

console.log(stringWithoutSpaces);
